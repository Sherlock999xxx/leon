{
  "$schema": "../../../schemas/tool-schemas/tool.json",
  "tool_id": "shell",
  "toolkit_id": "operating_system_control",
  "name": "Shell",
  "description": "Execute commands in the active platform shell. Uses Bash-compatible shell commands on Linux/macOS and PowerShell commands on Windows.",
  "icon_name": "terminal-box-line",
  "author": {
    "name": "Louis Grenard",
    "email": "louis@getleon.ai",
    "url": "https://twitter.com/grenlouis"
  },
  "functions": {
    "executeCommand": {
      "description": "Execute a command in the active platform shell and return the result.",
      "parameters": {
        "type": "object",
        "properties": {
          "command": {
            "type": "string"
          },
          "options": {
            "type": "object",
            "properties": {
              "cwd": {
                "type": "string"
              },
              "timeout": {
                "type": "number",
                "description": "Total allowed runtime. Use seconds by default. Set for commands likely to run longer than the default."
              },
              "timeoutUnit": {
                "type": "string",
                "enum": [
                  "seconds",
                  "milliseconds"
                ],
                "description": "Explicit unit for timeout. Use seconds for intentionally long tasks."
              },
              "timeoutRetries": {
                "type": "number",
                "description": "Timeout-only retries. For expected long runs, prefer one realistic timeout with 0 retries."
              },
              "captureOutput": {
                "type": "boolean"
              }
            },
            "additionalProperties": false
          }
        },
        "required": [
          "command"
        ]
      }
    }
  }
}
